# Research Agent — 简历 STAR 描述库

> 用途：本项目的简历描述唯一来源。按岗位方向（大模型开发 / AI 应用）各备一份，
> 每条均可直接粘贴；全部内容可溯源（spec / wiki / commit / 实测日志），禁止虚标。
> 维护纪律：每个 Phase 关闭时同步更新本文件（Reverse Sync）。
> 更新至：2026-08-29（P0 求解链路工具化：新增 s4l_solve_run / s4l_field_extract / reproduce_s4l，s4l_model 扩展仿真设置与头模预检；工具 15→18；测试口径刷新 236/227；真实求解验收 D3 待 S4L 环境恢复后补）。

## 一句话定位（2026.07–08，持续迭代）

科研智能体：以自研 Agent 框架为引擎、ReAct 循环编排文献检索/论文参数抽取/仿真建模/科学求解工具链，打通"论文 → 仿真参数 → Sim4Life 头模建模 → 有损求解 → 定量场验证"的科研自动化闭环，并具备跨会话记忆与调用级评估能力。

## 项目主叙事（STAR，B 阶段核心故事）

**S/T**：电磁仿真（TMS 经颅磁刺激）科研复现链路长、商业软件（Sim4Life）自动化难度大——学生版 license 限制 headless 求解、官方 API 文档稀缺、试错成本高（单次 GUI 冷启动 3–5 分钟）。目标：让 Agent 自主完成从论文参数到头模电磁场验证的全链路。

**A**：
1. 逆向 license 机制（15 轮排查）：定位学生版为"云端会话授权"——仅 GUI 进程持有 QS 求解许可，headless 回退本地旧激活；实测否定 3 种绕过方案后，发现 `Sim4LifeLight.exe --minimized --run` 官方脚本入口，脚本在 GUI 进程内继承 license，实现全流程无人工求解。
2. 用排除法做验证路线决策：实验证明"空气域解析验证"与 MQS 求解器设计域天然冲突（无损介质 nothing to solve）后，改道"基准复算"——加载已知正确模型原样重跑，与基准结果逐体素对比。
3. 三次假设-实验循环定位 API 缺陷：对"激励写入不生效"依次排除属性直写 / Property 对象 / 结果封存三个假设（每轮均有读回验证日志），最终定性为 GUI 会话设置层只读，给出 Input.h5 绕行路线并显式记录边界，不伪造验证结果。
4. 37 轮探针攻克头模体素化：对"脚本建的组织体无法体素化"这一阻断性问题，用受控单变量实验法逐层剥离——BMEN16 正确模型重体素化 A/B 对照（管线正常）→ 注入实验（断点在我方实体）→ 组件绑定状态对照（缺网格纳入）→ 单位审计（document 默认毫米、几何小 1000 倍），最终定量闭环：三层组织体素计数与几何体积理论值逐一吻合。
5. 首个有损真实求解：论文参数 figure8 线圈 + 三层球壳头模（ITIS 组织电导率），MQS 涡流求解 6 迭代收敛；h5py 直读 Output.h5 提取焦点线场剖面，以"焦点 E/电流比 × 脉冲电流"线性换算做量级合理性校验。

**R**：
- 基准复算定量验证通过——E 场三分量与基准 max 相对差 0.000%（约 79 万强场体素/分量），管线级精确复现；
- 头模链路全通——材料数据库链接/网格纳入/分层体素化/有损求解/场提取全自动，求解器 6 迭代收敛；
- 场型物理合理——焦点线单峰、表面峰值向脑内单调衰减，论文 20mm 目标深度落在衰减区，5kA 等效焦点场 ≈96 V/m 正处 TMS 刺激阈值量级；
- 40+ 轮实测沉淀 40 条踩坑清单为可复用知识库（skill）；全链路各环节均有测试或实测证据。

## 分岗位粘贴版

### 大模型开发工程师向

- **论文→仿真参数桥**：实现 arXiv 文献工具链与论文理解子代理（agent-as-tool），LLM 抽取仿真参数并以"schema 校验 + 原文引句 + 失败局部重试"三道闸防编造，引句滑窗匹配适配 PDF 断词等真实噪声。
- **Agent 工具编排扩展**：设计 BackendTask→CompilerRegistry 的合成底盘，论文抽取参数经编译器映射为 Sim4Life 建模脚本（figure8 线圈/三层头模/材料数据库链接全自动生成），端到端复现 TMS 论文场景。
- **跨会话语义记忆检索**：wiki 记忆层从关键词计数升级为 chroma 本地向量索引（ONNX MiniLM）+ 关键词 RRF 混合排序，写入即同步索引、chromadb 异常自动降级纯关键词（零回归），agent 跨会话记忆召回不再依赖精确用词；embedding function 依赖注入，测试 mock 实现 CI 零下载。
- **评估与实证文化**：为仿真链路建立多级 ground truth（基准复算 ≤1% / 体素计数-几何体积定量吻合 / 场型特征判定），所有 Agent 产出以结构化断言闭环验证；30 次工具调用成功率实测 100%。
- **论文→Sim4Life 求解全自动编排工具链（18 工具）**：将"论文→抽参→头模建模→有损求解→场验证"沉淀为三层工具——论文抽参缺必需字段即结构化报错给 agent 带 focus 重试或显式声明 assumptions（不静默填充，防编造）；求解编排分 stage（编译 / headless 预检 fail-fast / GUI --run / Output.h5 场提取 / 两级验证），有参照走逐体素定量锚（≤1%）、无参照降级为特征判据（单峰/单调衰减/量级区间），复现报告显式列出假设声明（真实求解验收待 S4L 环境恢复后补）。

### AI 应用工程师向

- **商业仿真软件全自动求解**：逆向 Sim4Life 学生版云端会话 license 机制，落地 `GUI --run` 执行器模式（日志写文件 / 进程树超时清理 / 单座串行纪律），headless 无法求解的限制下实现零人工干预的有损求解与结果提取（h5py 直读 Output.h5，绕过后处理 API）。
- **深度调试与根因分析**：37 轮受控探针定位头模体素化失败的三层叠加根因（网格不纳入实体 / 默认体素引擎不兼容脚本实体 / 文档默认毫米致几何小千倍），建立"受控单变量 + 读回验证 + 已知正确模型 A/B 对照"的调试方法论；顺手修复潜伏一整期的单位 bug。
- **可靠性工程**：为求解编排设计单文件备份 + mtime 差分定位 + 分 stage 结构化错误（baseline/run/solve/perturb/locate），一次磁盘事故后复盘修复备份策略（71G→单文件级）并补回归测试。
- **分层测试体系**：测试集 236 collected / 227 非冒烟绿（契约/发射器内容/合成 fixture/回归保护逐字节比对）+ 真实冒烟分级（headless 体素化冒烟 25s / GUI 求解冒烟，5 个冒烟/e2e 文件 + 契约内嵌冒烟类），B2 已验证路径行为零回归。
- **商业软件求解链路产品化**：把已验证一次的手工求解链路沉淀为可复用工具链——GUI 包装三件套（日志写文件/进程树超时清理/单座串行纪律）+ headless 预检 fail-fast（不占 license 单座即可拦截配方错误）+ 求解产物磁盘保有策略，配合纯 h5py 零 license 的场提取与特征判定工具，227 非冒烟测试全绿（真实求解验收待 S4L 环境恢复后补）。

## 量化指标速查（全部实测）

| 指标 | 数值 | 证据 |
|---|---|---|
| 基准复算精度 | E 场 max 相对差 0.000%（≤1% 判 pass） | S4lSolveSmoke PASSED |
| 对比体素规模 | ~79 万强场体素/分量 × 3 分量 | h5py 逐体素统计 |
| 有损求解 | MQS 6 迭代收敛，GUI --run 全程 420s | 求解器日志 |
| 网格收敛性 | 1mm vs 2mm：焦点深度 E 相对差 4.60%（<5% 判收敛） | 复算对比 |
| 焦点场量级 | E/I = 1.93e-2 V/m/A @3kHz，5kA 等效 ≈96 V/m | 焦点线剖面提取 |
| 头模体素 | 脑 27.3 万/颅骨 7.8 万/头皮 6.4 万，与几何体积理论值吻合 | S4lHeadSmoke |
| 实测迭代 | license+API+体素化排查 70+ 轮，沉淀踩坑 40 条 | spec Execute Log / skill |
| 测试规模 | 236 collected / 227 非冒烟绿（5 个冒烟/e2e 文件 + 契约内嵌冒烟类除外），B3/B4 相关 53/53 绿 | pytest |
| 工具规模 | 18 个注册工具（文献/仿真/求解编排/场验证/wiki 记忆） | `tools/__init__.py` 注册表 |
| 工具调用成功率 | 30/30 = 100% | experiment tracking JSONL |

## 面试引导钩子（被追问时展开）

- "license 为什么不能绕过？"→ 云端会话授权 vs 本地激活的排查过程（改 ApplicationName/Ares 路径/产品串均被实测否定）。
- "怎么证明仿真结果是对的？"→ 三级 ground truth：基准复算（管线级精确复现）、体素计数 vs 几何体积（定量吻合）、场型物理合理性（量级校验）。
- "遇到 API 不工作怎么办？"→ 激励写入三假设排除法 + 读回验证模式 + 诚实边界声明。
- "最硬的一次调试？"→ 37 轮体素化排查：受控单变量实验设计、注入实验隔离变量、组件绑定对照发现"网格纳入"缺失、单位审计挖出文档默认毫米——以及"先读全部诊断日志再设计下一轮"的教训（答案第 1 轮就在日志里）。
- "Agent 生成的代码可信吗？"→ 发射器内容断言 + py_compile 守卫 + 逐字节回归保护 + headless/求解分级冒烟。
- "CI 有什么故事？"→ 给项目上 CI 的第一天就抓到 4 个被本地环境掩盖的潜伏 bug（skipUnless 前向引用被环境变量短路掩盖、鲁棒性测试未 mock 配置、skip 条件硬编码绝对路径、无密钥环境客户端构造即抛错）——"测试在本地绿不代表在干净环境绿"。
- "遇过数据/资产损坏吗？"→ BMEN16 基准模型修复案：基准 .smash 在 S4L 应用层打不开但 HDF5 容器完好（h5py 取证），定位为异常会话写入致损+基线 Output.h5 缺失双重损伤；从 S4L 崩溃恢复 sidecar 里找回 B3 验证时代的副本，基线 h5 用语义比对（同 Input 则同基线）确认后恢复，基准复算冒烟复活——教训：诊断探针要复用被测系统的既定执行协议，裸写探针会引入测量误差。
- "为什么不做论文数值对比？"→ 论文 2511.00744 未报告绝对场值（V/m），强行对比是伪精确；工程决策是降级为三级定性场型对比（焦点单峰/深度衰减/量级校验 96 V/m@5kA）+ 基准复算 0.000% 作为定量锚点——"不与论文数值强行挂钩"是经实验论证后显式记录的边界声明，不是没做出来。

## 溯源

- 规格与决策：本仓库 `mydocs/specs/`（本地，隐私边界不入库）
- 知识沉淀：`wiki/2026-08-04_09-03_B3闭环_*`、用户级 skill `sim4life`（40 条踩坑）
- 既有素材：`docs/resume/`（2026-07 版，A 阶段叙事）
