# Research Agent — 简历 STAR 描述库

> 用途：本项目的简历描述唯一来源。按岗位方向（大模型开发 / AI 应用）各备一份，
> 每条均可直接粘贴；全部内容可溯源（spec / wiki / commit / 实测日志），禁止虚标。
> 维护纪律：每个 Phase 关闭时同步更新本文件（Reverse Sync）。
> 更新至：2026-08-04（Phase B3 关闭，commit `ffcf844`）。

## 一句话定位（2026.07–08，持续迭代）

科研智能体：以自研 Agent 框架为引擎、ReAct 循环编排文献检索/论文参数抽取/仿真建模/科学求解工具链，打通"论文 → 仿真参数 → Sim4Life 建模 → 真实求解 → 定量验证"的科研自动化闭环，并具备跨会话记忆与调用级评估能力。

## 项目主叙事（STAR，B 阶段核心故事）

**S/T**：电磁仿真（TMS 经颅磁刺激）科研复现链路长、商业软件（Sim4Life）自动化难度大——学生版 license 限制 headless 求解、官方 API 文档稀缺、试错成本高（单次 GUI 冷启动 3–5 分钟）。目标：让 Agent 自主完成从论文参数到仿真验证的全链路。

**A**：
1. 逆向 license 机制（15 轮排查）：定位学生版为"云端会话授权"——仅 GUI 进程持有 QS 求解许可，headless 回退本地旧激活；实测否定 3 种绕过方案后，发现 `Sim4LifeLight.exe --minimized --run` 官方脚本入口，脚本在 GUI 进程内继承 license，实现全流程无人工求解。
2. 用排除法做验证路线决策：实验证明"空气域解析验证"与 MQS 求解器设计域天然冲突（无损介质 nothing to solve）后，改道"基准复算"——加载已知正确模型原样重跑，与基准结果逐体素对比。
3. 三次假设-实验循环定位 API 缺陷：对"激励写入不生效"依次排除属性直写 / Property 对象 / 结果封存三个假设（每轮均有读回验证日志），最终定性为 GUI 会话设置层只读，给出 Input.h5 绕行路线并显式记录边界，不伪造验证结果。

**R**：基准复算定量验证通过——E 场三分量与基准 max 相对差 0.000%（约 79 万强场体素/分量），管线级精确复现；30+ 轮实测沉淀 37 条踩坑清单为可复用知识库（skill）；全链路（论文→参数→建模→求解→验证）各环节均有测试或实测证据。

## 分岗位粘贴版

### 大模型开发工程师向

- **论文→仿真参数桥**：实现 arXiv 文献工具链与论文理解子代理（agent-as-tool），LLM 抽取仿真参数并以"schema 校验 + 原文引句 + 失败局部重试"三道闸防编造，引句滑窗匹配适配 PDF 断词等真实噪声。
- **Agent 工具编排扩展**：设计 BackendTask→CompilerRegistry 的合成底盘，论文抽取参数经编译器映射为 Sim4Life 建模脚本（figure8 双翼/单环模板），端到端复现 TMS 论文线圈优化场景。
- **评估与实证文化**：为仿真链路建立双 ground truth 判定（基准复算 ≤1% / 线性缩放 ±5% 三档 verdict），所有 Agent 产出以结构化断言闭环验证；30 次工具调用成功率实测 100%。

### AI 应用工程师向

- **商业仿真软件全自动求解**：逆向 Sim4Life 学生版云端会话 license 机制，落地 `GUI --run` 执行器模式（日志写文件 / 进程树超时清理 / 单座串行纪律），headless 无法求解的限制下实现零人工干预的仿真求解与结果提取（h5py 直读 Output.h5，绕过后处理 API）。
- **可靠性工程**：为求解编排设计单文件备份 + mtime 差分定位 + 分 stage 结构化错误（baseline/run/solve/perturb/locate），一次磁盘事故后复盘修复备份策略（71G→单文件级）并补回归测试。
- **分层测试体系**：默认测试集 140+ 用例（契约/发射器内容/合成 fixture/回归保护逐字节比对）+ 真实冒烟分级（skipUnless 环境），B2 已验证路径行为零回归。

## 量化指标速查（全部实测）

| 指标 | 数值 | 证据 |
|---|---|---|
| 基准复算精度 | E 场 max 相对差 0.000%（≤1% 判 pass） | S4lSolveSmoke PASSED |
| 对比体素规模 | ~79 万强场体素/分量 × 3 分量 | h5py 逐体素统计 |
| 模型规模 | BMEN16 头模 R1，3.6M dofs，求解 ~90s | 冒烟日志 |
| 实测迭代 | license+API 排查 30+ 轮，沉淀踩坑 37 条 | spec Execute Log / skill |
| 测试规模 | 默认集 140+，B3 相关 42/42 绿 | pytest |
| 工具调用成功率 | 30/30 = 100% | experiment tracking JSONL |

## 面试引导钩子（被追问时展开）

- "license 为什么不能绕过？"→ 云端会话授权 vs 本地激活的排查过程（改 ApplicationName/Ares 路径/产品串均被实测否定）。
- "怎么证明仿真结果是对的？"→ 空域解析验证判伪的推理 + 基准复算的双 ground truth 设计。
- "遇到 API 不工作怎么办？"→ 激励写入三假设排除法 + 读回验证模式 + 诚实边界声明。
- "Agent 生成的代码可信吗？"→ 发射器内容断言 + py_compile 守卫 + 逐字节回归保护 + 冒烟分级。

## 溯源

- 规格与决策：本仓库 `mydocs/specs/`（本地，隐私边界不入库）
- 知识沉淀：`wiki/2026-08-04_09-03_B3闭环_*`、用户级 skill `sim4life`（37 条踩坑）
- 既有素材：`docs/resume/`（2026-07 版，A 阶段叙事）
