# 素材溯源清单（防夸大自查）

每条简历 bullet 对应的实证来源。括号内为验证方式/位置。

## 量化数字

| 数字 | 来源 |
|---|---|
| 工具调用成功率 100%（30/30） | `artifacts/experiment_log.jsonl`（30 条实跑记录全 success=true）；`artifacts/scenario_report.md` |
| 17 个测试 | `python -m pytest tests/`（test_wiki 4 + test_tools_contract 7 + test_robustness 6） |
| headless 建模 3/3、平均 15.3s | experiment_log 中 `_s4l_run_script` 3 条记录（16.9/13.7/15.3s） |
| TMS 优化 4/4、平均 1.8s | experiment_log 中 `_tms_optimize` 4 条记录（1.99/1.39/1.60/1.60s） |
| sim4life_manual_qa 平均 76.6s | experiment_log 中 7 条 `_sim4life_manual_qa` 记录 |
| 跨会话记忆召回 5 条 | A4 验证：新 agent 会话 `wiki_search("TMS 线圈优化")` 返回 5 条（对话实测） |
| demo EXIT=0 | `scripts/demo_research.py` 运行记录（产出 wiki + demo_sphere.smash） |
| 兄弟项目测试数 37/678/405 | 各自仓库 pytest 实跑结果（2026-07-20/21 对话记录） |

## 技术点

| 声称 | 证据 |
|---|---|
| ToolDescriptor 元数据层 | `src/research_agent/descriptor.py`（category/cost_hint/requires/async_capable/produces_artifacts 五字段） |
| requires 启动自检识别 RAG 未启动 | `build_agent()` 实测返回 `{'sim4life_manual_qa': ['rag_service']}`（对话实测） |
| Function Calling 6 工具 | `tools/__init__.py` 注册列表 + `agent.tools._tools` 实测 7 个（含 s4l_write_script） |
| 消除 CurrentDocument 幻觉 | 首轮 demo 日志出现 `document.CurrentDocument()`（不存在）→ prompts.py 加最小模板后复跑通过（spec §7 记录） |
| schema/handler 参数契约修复 | `_tms_optimize` TypeError → 改可选默认值（spec §7 记录） |
| FlexNet 许可排查 | 对话实测：tsf.data 定位、lmgrd 1055 端口、run_application() 源码入口、headless 建球保存 .smash 成功 |
| RAG 宕机明确报错 | `tests/test_robustness.py::test_rag_down_raises_clear_error_not_hallucination` |
| TMS 超时返回 partial | `tests/test_robustness.py::test_tms_timeout_returns_structured_partial` |
| LLM 长连接挂起定位 | 多场景运行中两次 30 分钟挂起，经 experiment_log 逐工具耗时数据确认挂起点在 LLM 调用而非工具（scenario_report.md §观察3） |

## 诚实声明（不可用于简历的点）

- demo 的 LLM 路径耗时波动大（50s~>300s），不能说"稳定秒级完成"。
- s4l 目前只验证了建模保存，仿真提交未做端到端（接口已留）。
- M1 的 wiki 是关键词检索，向量语义检索属于 M2 规划，未实现。
- 多场景脚本因 LLM 挂起中断过两次，最终数据是"分场景补跑+日志汇总"得来，报告已如实注明。
