# B3 闭环：Sim4Life 基准复算验证 + 方案 A 边界（2026-08-04）

> 来源：research-agent Phase B3（spec `mydocs/specs/2026-08-01_phase-b3-s4l-solve.md`，本地未入库）。
> 本条为同步沉淀：结论 + 边界 + 下一步钩子。

## 结论（已验证）

- **数值链路可信**：agent 生成 GUI --run 重跑脚本 → Sim4LifeLight.exe 继承云端 license
  → iSolve 直跑（`run_isolve_directly=True`，Ares 路径报笼统错误）→ h5py 直读
  Output.h5 逐分量强场对比。BMEN16 R1（3.6M dofs）复算 vs 基准 **max 相对差 ≤1%
  （实测 ~0%）**，冒烟 `S4lSolveSmoke::test_benchmark_recompute_pass` PASSED。
- **工具化交付**：`tools/s4l_solve.py::_s4l_solve_benchmark`（严格串行，license 单座）；
  单文件备份（GetOutputFileName）+ 对比后自动清理；mtime 快照差分定位复算输出。
- **compiler 仿真级扩展**：`with_simulation` overrides 追加 CreateCircle 线框电流源
  + MQS 设置 + 求解段；B2 默认路径逐字节回归保护。

## 已知边界（诚实声明）

- **方案 A 激励扰动**：GUI 会话中加载模型的设置对象**整体不可写**（属性直写 /
  AmplitudeProp.Value / ClearResults 后三假设全部实验排除；headless 同对象可写）。
  线性验证留 B4，走 `WriteInputFile → h5py 改 Input.h5 → iSolve 直跑` 路线。
- 空气域 MQS "nothing to solve"：`with_simulation` 产物为资产脚本，数值验证不走该路径。
- E 场 edge-centered（分量 shape 交错）、~64% NaN 体素——对比必须逐分量 + 强场 mask + nan 安全。

## 下一步（B4 钩子）

1. 头模建模接入（组织电导率、MaterialSettings + 数据库链接）；
2. 方案 A'：Input.h5 激励修改路线（结构已侦察：
   `SolverSpecificSettings/EmLfPolylineCurrentSourceCollection/<uuid>/`）；
3. 论文数值对比（2511.00744）在头模环境下重启。

## 环境事实

- 基准：`D:\TMS\BME\BMEN16\BMEN16.smash`（19 个 MQS 仿真）；原始基线存档
  `research-agent/artifacts/h5_baseline_1785770498/`（13G，勿删）。
- QS_SOLVER 有效期至 2027-02-09；S4L 任务严格串行。
